enum ClockAction {
  initial,
  loading,
  idle,
  success,
  failed;
}

enum LoginStatus {
  success,
  failure,
  unknown,
}

enum ClickButton {
  none,
  clockInButton,
  clockOutButton,
}

enum Language {
  enUS,
  zhTW,
}
