enum ClockAction {
  initial,
  loading,
  idle,
  success,
  failed;
}

enum LoginStatus {
  success,
  failure,
  initial,
}

enum ClickButton {
  none,
  clockInButton,
  clockOutButton,
}

enum Language {
  enUS,
  zhTW,
}
